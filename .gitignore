# Build artifacts -- rebuilt by build.bat, never committed. The release zip
# is what gets attached to a GitHub Release, not committed to git history.
__pycache__/
build/
*.spec
*.exe
CookieRunAutoMenuBot-v*.zip

# Personal working captures -- not needed for the app to function,
# regenerate locally with the Save Screenshot button as needed.
debug_shots/

# Superseded single-file prototype, kept locally for reference only.
cookierun.py
